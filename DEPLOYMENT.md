# 河海大学课程表 - 部署和使用说明

## 概述

河海大学课程表是一个基于 Electron 的桌面应用程序，用于自动获取和显示河海大学教务系统的课程表信息。

## 系统要求

- **Windows**: Windows 10 或更高版本
- **macOS**: macOS 10.14 或更高版本  
- **Linux**: Ubuntu 16.04+ 或其他现代 Linux 发行版
- **内存**: 至少 2GB RAM
- **磁盘空间**: 至少 500MB 可用空间

## 快速开始

### 方式一：使用预构建版本（推荐）

1. **下载应用程序**
   - Windows: 下载 `河海大学课程表-1.0.0-x64.exe`（便携版）或安装程序
   - macOS: 下载 `.dmg` 文件
   - Linux: 下载 `.AppImage` 文件

2. **安装或运行**
   - **便携版**: 直接双击 `.exe` 文件运行，无需安装
   - **安装版**: 运行安装程序，按照向导完成安装
   - **macOS**: 打开 `.dmg` 文件，拖拽到应用程序文件夹
   - **Linux**: 添加执行权限后直接运行 `.AppImage`

3. **首次配置**
   - 启动应用后，点击"设置"按钮
   - 输入您的河海大学教务系统用户名和密码
   - 设置正确的学期开始日期
   - 保存配置

4. **获取课程表**
   - 点击"获取最新课表"按钮
   - 等待系统自动登录并获取课程信息
   - 课程表将自动显示当前周的课程安排

### 方式二：从源码构建

#### 环境准备

```bash
# 安装 Node.js (版本 16 或更高)
# Windows: 从 https://nodejs.org 下载安装
# macOS: brew install node
# Linux: 使用包管理器安装 nodejs 和 npm
```

#### 构建步骤

1. **克隆或下载源代码**
   ```bash
   git clone <repository-url>
   cd course-schedule
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **运行开发版**
   ```bash
   npm start
   ```

4. **构建发布版**
   
   **Windows**:
   ```bash
   # 使用构建脚本
   build.bat
   
   # 或手动构建
   npm run build:win
   ```
   
   **macOS**:
   ```bash
   # 使用构建脚本
   ./build.sh
   
   # 或手动构建
   npm run build:mac
   ```
   
   **Linux**:
   ```bash
   # 使用构建脚本
   ./build.sh
   
   # 或手动构建
   npm run build:linux
   ```

5. **查找构建结果**
   - 构建完成后，在 `dist/` 目录中查找生成的文件
   - Windows: `.exe` 文件（便携版和安装程序）
   - macOS: `.dmg` 文件
   - Linux: `.AppImage` 和 `.deb` 文件

## 应用功能

### 主要功能
- **自动登录**: 自动登录河海大学教务系统
- **课表获取**: 获取指定周次的课程安排
- **实时显示**: 显示课程详细信息，包括时间、地点、教师等
- **周次导航**: 可查看不同周次的课程安排
- **当前课程提醒**: 高亮显示下一节课
- **本地缓存**: 支持离线查看已获取的课程表

### 界面操作
- **获取最新课表**: 从教务系统获取当前周课程表
- **本地课表**: 加载本地缓存的课程表
- **设置**: 配置登录信息和系统参数
- **周次选择**: 切换查看不同周次的课程
- **上一周/下一周**: 快速切换相邻周次
- **当前周**: 快速跳转到当前周

## 配置说明

### 配置文件位置
- **Windows**: `%APPDATA%/hhu-course/config.json`
- **macOS**: `~/Library/Application Support/hhu-course/config.json`
- **Linux**: `~/.config/hhu-course/config.json`

### 配置参数
```json
{
  "username": "您的学号",
  "password": "您的密码",
  "semesterStart": "2024-09-02",
  "autoLogin": false,
  "windowBehavior": "minimize"
}
```

- **username**: 河海大学教务系统用户名（学号）
- **password**: 教务系统密码
- **semesterStart**: 学期开始日期（格式：YYYY-MM-DD）
- **autoLogin**: 是否自动登录（暂未启用）
- **windowBehavior**: 窗口行为设置

## 数据文件

### 课程数据存储
- **位置**: 与配置文件相同目录下的 `course_info.json`
- **内容**: 包含所有已获取的课程表数据
- **格式**: JSON 格式，按周次组织

### 登录状态
- **位置**: 与配置文件相同目录下的 `login_state.json`
- **内容**: 保存登录会话信息，用于免重复登录

## 故障排除

### 常见问题

1. **无法启动应用**
   - 确保系统满足最低要求
   - 检查杀毒软件是否误报
   - 尝试以管理员身份运行

2. **登录失败**
   - 检查用户名和密码是否正确
   - 确认教务系统是否可以正常访问
   - 检查网络连接

3. **课程表为空**
   - 确认学期开始日期设置正确
   - 检查当前周次是否在有效范围内（1-25周）
   - 尝试获取其他周次的课程表

4. **界面无响应**
   - 按 F5 刷新界面
   - 按 Escape 重置界面状态
   - 重启应用程序

### 调试功能
- **F1**: 显示帮助信息
- **F5**: 刷新页面
- **Escape**: 重置界面
- **Ctrl+Shift+I**: 打开开发者工具（调试用）

## 数据迁移

### 迁移到新电脑

1. **备份数据**（可选）
   - 复制配置目录中的 `config.json` 和 `course_info.json`
   
2. **在新电脑上安装**
   - 运行便携版或安装程序
   
3. **恢复数据**（可选）
   - 将备份的文件放置到新电脑的配置目录中
   - 或重新配置并获取课程表

### 重置应用
如需重置应用到初始状态，删除整个配置目录即可。

## 更新说明

### 自动更新
- 目前版本不支持自动更新
- 需要手动下载新版本进行更新

### 手动更新
1. 下载新版本的应用程序
2. 备份现有配置（可选）
3. 安装或替换旧版本
4. 配置会自动保留

## 技术支持

如遇到问题，请按以下步骤操作：

1. **查看日志**: 使用 Ctrl+Shift+I 打开开发者工具查看控制台日志
2. **重置配置**: 删除配置目录重新设置
3. **检查网络**: 确认能正常访问河海大学教务系统
4. **联系开发者**: 提供错误信息和操作步骤

## 隐私说明

- 应用仅在本地存储您的登录信息和课程数据
- 不会向第三方服务器传输您的个人信息
- 登录凭据仅用于访问河海大学教务系统

## 开源许可

本项目使用 MIT 许可证，详情请查看 LICENSE 文件。

---

**版本**: 1.0.0  
**更新日期**: 2024年12月
# 河海大学课程表应用 - 使用说明

## 🎯 项目功能
这是一个自动化工具，用于：
- 自动登录河海大学教务系统
- 获取课程表信息
- 解析并显示课程安排
- 保存课程数据到本地

## 🚀 启动方式

### 推荐方式：使用PowerShell脚本
```powershell
.\启动应用.ps1
```

### 备用方式：
1. 使用PowerShell直接启动：`.\start.ps1`
2. 使用批处理文件：`.\start-fixed.bat`
3. 直接命令：`node node_modules/electron/cli.js .`

## ⚠️ 关于终端乱码问题

### 问题说明
Windows系统下的PowerShell/CMD在显示中文时可能出现乱码，这是Windows编码系统的已知问题。

### 解决方案
1. **应用功能完全正常**：虽然终端显示可能有乱码，但应用的核心功能（登录、获取课程、保存数据）完全正常
2. **数据正确存储**：所有课程信息都正确保存在 `course_info.json` 文件中，中文内容完全正常
3. **界面显示正常**：应用的图形界面中文显示完全正常

### 验证方法
打开 `course_info.json` 文件，您会看到完整的中文课程信息，例如：
```json
{
  "name": "三维协同设计与BIM技术",
  "location": "北教201",
  "class": "水工22-1（水工）班"
}
```

## 📋 应用状态说明

### 正常运行标志
- 显示 "Login successful, redirected"
- 显示 "Course table page loaded"
- 显示课程数量和保存信息
- 生成或更新 `course_info.json` 文件

### 文件说明
- `course_info.json` - 课程数据存储文件
- `login_state.json` - 登录状态缓存（2小时有效期）
- `debug_page.html` - 调试页面（出错时生成）

## 🔧 故障排除

### 如果遇到登录问题
1. 删除 `login_state.json` 文件强制重新登录
2. 检查网络连接
3. 确认教务系统是否正常访问

### 如果课程数据为空
1. 检查当前周是否有课程安排
2. 确认登录的账号权限
3. 查看 `debug_page.html` 文件了解详情

## 💡 技术说明
- 使用 Electron + Node.js 开发
- 使用 Puppeteer 进行网页自动化
- 支持登录状态缓存和课程数据本地存储
- 自动处理登录验证和会话管理

## 📞 重要提示
- 应用完全在本地运行，不会上传任何个人信息
- 登录凭据仅用于教务系统访问
- 建议定期更新课程数据

---
*如果终端显示乱码，请忽略，专注于应用功能本身。所有核心功能都正常工作！*
